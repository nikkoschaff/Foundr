<%= simple_form_for @role do |f| %>

  <div class="field">
    <%= f.label :picture, :as => "Profile Picture" %>
    <%= f.file_field :picture, :as => "Profile Picture" %>
  </div>

  <%= f.simple_fields_for :profile do |profile| %>
    <fieldset>
      <div class="field">
        <%= profile.input :name %>
      </div>
    </fieldset>

    <fieldset>
      <div class="field">
        <%= profile.input :headline %>
      </div>
    </fieldset>

    <fieldset>
      <div class="field-about">
        <%= profile.input :about %>
      </div>
    </fieldset>

    <fieldset>
      <div class="field">
        <%= profile.association :ambitions, :as => :check_boxes %>
      </div>
    </fieldset>

    <fieldset>
      <div class="field-skills">
        <%= profile.input :tag_list, :size => 150 %>
      </div>
    </fieldset>

  <% end %>

  <div class="field">
    <%= f.input :email, :required => false %>
  </div>

  <%= f.hidden_field :password, :value => @role.password  %>
  <%= f.hidden_field :password_confirm, :value => @role.password %>

  <div class="actions">
    <%= f.button :submit, :as => "Update profile", :class => "btn btn-primary" %>
  </div>
<% end %>